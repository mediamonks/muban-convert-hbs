@foreach (var users_i in users)
{
  if (users_i.foo)
  {
    foreach (var comments_i in users_i.comments)
    {
      if (comments_i.title)
      {
        <a href="@comments_i.id">@comments_i.name</a>
      }
    }
  }
  else
  {
    foreach (var comments_i in users_i.comments)
    {
      comments_i.id
    }
  }
}